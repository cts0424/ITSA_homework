#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int total = 0;

    for(int i = 6 ; i < n ; i += 6){
        if(i % 12 == 0){
            total += 0;
        }else{
            total += i;
        }
    }

    printf("%d\n",total);

    return 0;
}
