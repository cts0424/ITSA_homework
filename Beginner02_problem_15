#include <stdio.h>

int main() {
    int n;
    scanf("%d",&n);

    double temp;

    if(n >= 1500){
        temp = n * 0.79;
    }else if(n >= 800){
        temp = n * 0.9;
    }else{
        temp = n;
    }

    temp *= 0.9;
    printf("%0.1lf\n",temp);

    return 0;
}
