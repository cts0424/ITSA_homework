#include <stdio.h>
#include <math.h>

int main(){
    double x,y,temp;
    double squared_r = 100*100;
    
    scanf("%lf %lf",&x,&y);

    double squared_distance = x*x + y*y;

    if(squared_r >= squared_distance){
        printf("inside\n");
    }else{
        printf("outside\n");
    }

    return 0;

}
