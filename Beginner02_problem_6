#include <stdio.h>

int main(){
    int n;
    int x,y,temp;

    scanf("%d",&n);
    for(int i=0 ; i<n ; i++){
        scanf("%d %d",&x,&y);
        temp = x+y;
        printf("%d\n",temp*temp);
    }

    return 0;
}
